{
  "name": "modelkeep",
  "version": "0.0.1",
  "workspaces": [
    "packages/*"
  ],
  "description": "3D Printing Business managing tool, with Model Library and Printing Cue",
  "productName": "ModelKeep",
  "author": "Morialkar (philippe@morialkar.com)",
  "private": true,
  "scripts": {
    "models:generate": "bun run --cwd packages/models generate",
    "models:studio": "bun run --cwd packages/models studio",
    "api:dev": "bun run --cwd packages/api offline",
    "admin_client:dev": "bun run --cwd packages/admin_client dev"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^4.5.4"
  },
  "engines": {
    "node": "^18 || ^16 || ^14.19"
  }
}